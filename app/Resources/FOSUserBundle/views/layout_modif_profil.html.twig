{% extends "template/cadreUser.html.twig" %}

{% block form_profil %}
    <div class="col-xs-1 col-sm-1 col-md-1 col-lg-1 col-xl-1">

    </div>
    
        <h1 class="text-center">Modifier le profil</h1>

    
    <div class="marge-haut">


        {% if app.request.hasPreviousSession %}
            {% for type, messages in app.session.flashbag.all() %}
                {% for message in messages %}
                    <div class="flash-{{ type }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        {% endif %}

        <div>
            {% block fos_user_content %}
            {% endblock %}
        </div>
 {#exécuté et inclure URL d'inscription et l'inclure dans la page
{{ render(path("fos_user_registration_register")) }#}


</div>
{% endblock %}
