{% extends "base.html.twig" %}

{% block nav %} 

   <nav class="navbar navbar-expand-md bg-secondary navbar-dark fixed-top">

    <!-- Brand -->

        <a class="navbar-brand" href="{{ path('acceuil_homepage') }}">

            <img src="{{ asset('images/Gestion_de_stock.png') }}" alt=logo" class="logo">


             <strong>Stock-En-Ligne</strong>

       </a>
      
   

  <!-- Toggler/collapsibe Button -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
    
 <span class="navbar-toggler-icon"></span>
  </button>

  <!-- Navbar links -->
  <div class="collapse navbar-collapse text-blanc" id="collapsibleNavbar">
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('fos_user_profile_show') }}">Profil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('fos_user_profile_edit') }}">Modifier Profil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path ('fos_user_change_password') }}">Changer Mot de Passe</a>
            </li> 
            <li class="nav-item">
                <a class="nav-link" href="{{ path ('fos_user_security_logout') }}">Déconnexion</a>
            </li> 
        </ul>

        
    {% else %}   
        {{ render(path('fos_user_security_login')) }}
    {% endif %}    
  </div> 
</nav>

{% endblock %}


{% block body %} 

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
<!--sm (>=576px), md (>=768px), lg (>=992px) or xl (>=1200px) -->
    <div class="marge col-xs-2 col-sm-2 col-md-2 col-lg-2 col-xl-2 bg-info">
    {% else %}
<!--sm (>=576px), md (>=768px), lg (>=992px) or xl (>=1200px) -->
    <div class="marge marge-haut col-xs-2 col-sm-2 col-md-2 col-lg-2 col-xl-2 bg-info">
    {% endif %}

        <!-- A vertical navbar -->
        <nav class="navbar bg-light">

            <!-- Links -->
            <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('acceuil_homepage') }}">Accueil</a>
                    </li>           
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('art_index') }}">Articles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('cli_index') }}">Clients</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('four_index') }}">Fournisseurs</a>
                    </li>                

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('ach_index') }}">Achats</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('achadet_index') }}">Achat details</a>
                    </li>
                  

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('vent_index') }}">Ventes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('ventdet_index') }}">Vente details</a>
                    </li>
                {% endif %}  

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('apropos_homepage') }}">A propos</a>
                    </li>                
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('contact_homepage') }}">Contact</a>
                    </li> 
            </ul>

        </nav>
        <br>
    </div>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="container-fluid">
    {% else %}  
        <div class="container-fluid marge-haut">
    {% endif%}
            {% block form_reset %}{% endblock form_reset %}
            {% block form_check_email %}{% endblock form_check_email %}
            {% block form_reset_request %}{% endblock form_reset_request %}
            {% block form_changer_mdp %}{% endblock form_changer_mdp %}
            {% block form_inscription %}{% endblock form_inscription %}
            {% block form_profil %}{% endblock form_profil %}

            {% block table %} {% endblock table %} 
        </div>
        

{% endblock %}


{% block footer%}

   <nav class="navbar navbar-expand-md bg-secondary navbar-secondary">
      <!--Brand-->
<p>Version 0.1.60 Alpha Copyright © 2019-2020 All Rights Reserved. Designed by
       <a class="navbar-brand" href="https://www.facebook.com/mannou.fezaii">Manel Fezai</a> </p>


{% endblock %}






